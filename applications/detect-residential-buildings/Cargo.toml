[package]
name = "detect-residential-buildings"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../../components/common" }
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.8", features = [ "runtime-tokio", "postgres" ] }
log = "0.4.22"
